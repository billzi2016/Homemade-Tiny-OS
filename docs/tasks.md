# Tasks

## 使用规则

- 所有开发任务默认遵守 TDD：先写测试，再写实现，再做重构
- 没有对应测试的功能，不应标记完成
- 一个任务如果过大，应继续拆成更小的可验证任务

## Phase 1: 项目骨架与测试基线

- [x] 建立 `src/tinyos/` 基础包结构
- [x] 建立 `tests/unit`、`tests/integration`、`tests/e2e` 目录
- [x] 先写配置默认值测试
- [x] 实现配置对象与默认参数
- [x] 先写非法配置拒绝测试
- [x] 实现配置校验逻辑
- [x] 先写统一错误类型测试
- [x] 实现基础异常层级
- [x] 先写核心对象可构造测试
- [x] 定义 `ExpandableVirtualDisk`、`VirtualFileSystem`、`KernelScheduler`、`Shell`、`KernelStats` 骨架
- [x] 统一测试运行方式并写入说明

## Phase 2: 可扩展虚拟磁盘与块设备

- [x] 先写新磁盘初始化测试
- [x] 实现磁盘镜像创建与元信息初始化
- [x] 先写已有镜像重新打开测试
- [x] 实现磁盘镜像恢复打开逻辑
- [x] 先写块读写测试
- [x] 实现 `read_block()` 与 `write_block()`
- [x] 先写块分配测试
- [x] 实现 `allocate_blocks()` 与 `free_blocks()`
- [x] 先写单次扩容测试
- [x] 实现按步长扩容逻辑
- [x] 先写超过上限拒绝测试
- [x] 实现 `DiskFullError` 路径
- [x] 先写关闭后恢复测试
- [x] 实现 `flush()` 与恢复一致性

## Phase 3: 虚拟文件系统与元数据索引

- [x] 先写路径规范化测试
- [x] 实现绝对路径、相对路径、`.`、`..` 解析
- [x] 先写根目录初始化测试
- [x] 实现根目录与 `SuperBlock` 初始化
- [x] 先写 `Inode` 和 `DirectoryEntry` 基础测试
- [x] 实现核心元数据模型
- [x] 先写 `DirectoryIndex` 抽象接口测试
- [ ] 选择并封装一个成熟索引库作为首版目录索引后端
- [x] 先写目录创建测试
- [x] 实现 `mkdir(path)`
- [x] 先写文件创建与读取测试
- [x] 实现 `create_file()` 与 `read_file()`
- [x] 先写覆盖写和追加写测试
- [x] 实现 `write_file(path, data)` 的覆盖与追加语义
- [x] 先写跨块大文件测试
- [x] 实现文件块映射逻辑
- [x] 先写删除与块回收测试
- [x] 实现 `delete_file(path)`
- [x] 先写重启恢复测试
- [x] 实现文件系统元数据与目录索引恢复逻辑
- [x] 增加目录项压力测试

## Phase 4: 进程调度、Shell 与系统内省

- [x] 先写 PCB 状态流转测试
- [x] 实现 `PCB` 基础模型
- [x] 先写调度器轮转测试
- [x] 实现 Round-Robin 调度器
- [x] 先写进程退出测试
- [x] 实现 `ZOMBIE` 清理与队列维护
- [x] 先写 Shell 命令解析测试
- [x] 实现命令解析器
- [x] 先写 `ls`、`cd`、`mkdir`、`cat` 集成测试
- [x] 实现首批文件系统命令
- [x] 先写 `ps`、`kill` 命令测试
- [x] 实现进程管理命令
- [x] 先写 `top` 输出快照测试
- [x] 实现内核监控视图
- [x] 先写 `sysstat` 输出测试
- [x] 实现系统资源视图
- [x] 先写 `dmesg` 环形缓冲区测试
- [x] 实现内核日志缓冲区

## Phase 5: 可靠性、权限与可扩展能力

- [x] 先写磁盘写满测试
- [x] 完善空间不足错误处理与 Shell 提示
- [x] 先写普通用户权限拒绝测试
- [x] 实现基础权限检查
- [x] 先写 root 放行测试
- [x] 实现 root 特权路径
- [x] 先写损坏元数据识别测试
- [x] 实现超级块签名、版本号或等价校验机制
- [x] 先写异常恢复测试
- [x] 实现最小检查点或一致性恢复机制
- [x] 先写索引实现替换兼容性测试
- [x] 验证目录索引适配层可替换
- [x] 先写命令注册扩展测试
- [x] 实现插件式命令注册接口

## 跨阶段任务

- [ ] 维护 `doc-tree.md`
- [ ] 每完成一个 Phase，同步回写 `tiny-os-prd.md`
- [ ] 每新增用户可见行为，同步补充回归测试
- [ ] 每新增异常路径，同步补充错误输出测试
